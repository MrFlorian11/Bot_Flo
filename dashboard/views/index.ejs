<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Bot Dashboard</title>
  <link rel="stylesheet" href="/public/style.css"/>
</head>
<body>

<header class="header">
  <div class="header-inner">
    <div class="brand">
      <div class="logo"><span>⚙️</span></div>
      <div>Bot Dashboard</div>
    </div>

    <div class="nav">
      <% if (user) { %>
        <div class="userpill">
          <% if (user.avatar) { %>
            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png?size=64" alt="">
          <% } %>
          <span class="name"><%= user.username %></span>
        </div>
        <a href="/logout">Se déconnecter</a>
      <% } else { %>
        <a class="btn primary" href="/login">Se connecter avec Discord</a>
      <% } %>
    </div>
  </div>
</header>

<div class="container">
  <h1>Mes serveurs</h1>

  <% if (!user) { %>
    <div class="card"><p>Connecte-toi pour afficher et configurer tes serveurs.</p></div>
  <% } else if (!guilds || guilds.length === 0) { %>
    <div class="alert warn">Aucun serveur géré trouvé. Vérifie que tu possèdes la permission <strong>Gérer le serveur</strong>.</div>
  <% } else { %>
    <ul class="guilds">
      <% guilds.forEach(g => { %>
        <li>
          <a class="guild" href="/guild/<%= g.id %>">
            <div class="icon"><%= (g.icon ? '' : g.name[0].toUpperCase()) %></div>
            <div class="meta">
              <div style="font-weight:700"><%= g.name %></div>
              <code><%= g.id %></code>
            </div>
          </a>
        </li>
      <% }) %>
    </ul>
  <% } %>
</div>

<footer>© <%= new Date().getFullYear() %> — Ton Bot</footer>
</body>
</html>
